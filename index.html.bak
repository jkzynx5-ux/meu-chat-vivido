<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Chat Vivido Pro</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }
        
        body { 
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            height: 100vh; display: flex; justify-content: center;
        }

        #app-container {
            width: 100%; max-width: 500px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(15px);
            display: flex; flex-direction: column;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
        }

        header { 
            padding: 20px; background: rgba(255, 255, 255, 0.1);
            color: white; border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
        }

        #messages { 
            flex-grow: 1; overflow-y: auto; padding: 20px;
            display: flex; flex-direction: column; gap: 12px;
        }

        li { 
            list-style: none; max-width: 80%; padding: 12px 16px; 
            font-size: 15px; animation: fadeIn 0.3s ease;
            position: relative;
        }

        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        .other { align-self: flex-start; background: white; color: #333; border-radius: 20px 20px 20px 5px; }
        .me { align-self: flex-end; background: #0088cc; color: white; border-radius: 20px 20px 5px 20px; }

        .username { display: block; font-size: 11px; font-weight: bold; margin-bottom: 4px; color: #6ab3f3; }
        .me .username { color: #b3e5fc; }

        #typing-indicator { padding: 5px 20px; color: #6ab3f3; font-size: 13px; font-style: italic; min-height: 25px; }

        /* Painel de Emojis */
        #emoji-picker {
            display: none; background: rgba(255, 255, 255, 0.1);
            padding: 10px; grid-template-columns: repeat(6, 1fr); gap: 10px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .emoji-btn { font-size: 24px; background: none; border: none; cursor: pointer; }

        #form { padding: 15px; background: rgba(255, 255, 255, 0.05); display: flex; gap: 10px; align-items: center; }
        
        #toggle-emoji { background: none; border: none; font-size: 22px; cursor: pointer; opacity: 0.7; }

        #input { 
            flex-grow: 1; background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 25px; padding: 12px 20px; color: white; outline: none;
        }

        #btn { background: #0088cc; color: white; border: none; width: 45px; height: 45px; border-radius: 50%; font-weight: bold; }
    </style>
</head>
<body>
    <div id="app-container">
        <header><h2 id="user-display">Chat Premium</h2></header>
        <ul id="messages"></ul>
        <div id="typing-indicator"></div>

        <div id="emoji-picker">
            <button class="emoji-btn">😀</button><button class="emoji-btn">😂</button>
            <button class="emoji-btn">😍</button><button class="emoji-btn">🔥</button>
            <button class="emoji-btn">🙌</button><button class="emoji-btn">👍</button>
        </div>

        <form id="form">
            <button type="button" id="toggle-emoji">😊</button>
            <input id="input" autocomplete="off" placeholder="Mensagem..." />
            <button id="btn">></button>
        </form>
    </div>

    <script src="/